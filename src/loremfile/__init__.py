"""loremfile — generators, validators and tooling for loremfile.dev.

The package is installed with ``pip install -e .`` inside the pinned toolchain
image (``tools/TOOLCHAIN_DIGEST``). Only that image's output is authoritative:
host builds differ for media and images and will fail the manifest lock check.
"""

__version__ = "0.1.0"
