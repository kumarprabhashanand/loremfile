"""Validator dispatch by format, plus the checks common to every family.

Implemented in M3 alongside the generators: every generator family ships with its
validator, a negative test and a determinism test in the same pull request
(docs/06 §13, docs/15 M3).
"""
