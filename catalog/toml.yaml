# toml fixtures (docs/05 §3.7). Launch-set rows only; the rest are P1b.
format: toml
family: data
mime: application/toml
description: >-
  A configuration file covering TOML's tables, array-of-tables, multi-line and
  literal strings, and all four date-time types.
seo_title: "Sample TOML config file for testing parsers — free, CC0"
related: [yaml, json, ini]
fixtures:
  - name: config.toml
    phase: 1
    generator: data.toml_config
    params: {}
    description: "Tables, nested tables, an array of tables, multi-line basic and literal strings, underscored integers, and offset, local, date and time values."
    tags: [data]
    expect: { documents: 1 }
    size_class: free
